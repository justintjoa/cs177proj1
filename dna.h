#include <iostream>
#include <vector>
using namespace std;


class DNAreader {
	public:
		DNAreader(string a, string b, double m, double c, double d);
		~DNAreader();


	private:	
		vector<string> collection;
		int similarity;
		int height;
		int width;

};